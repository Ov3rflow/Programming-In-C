/* Chapter 2 - Exercise 5
    Fix the code (All fixes have been commented)*/
#include <stdio.h>

//void was capitalized (Void)
int main(void)
{
    //int was capitalized (INT)
    int sum;

    //The comment line wasn't closed off at all
    /*COMPUTE RESULT */

    //There was no semicolon at the end of the line
    sum = 25 + 37 - 19;

    //The comment line wasn't closed off properly
    /*DISPLAY RESULTS */

    //The comma was missing after the closing "
    printf("The answer is %i\n", sum);

    return 0;

}

